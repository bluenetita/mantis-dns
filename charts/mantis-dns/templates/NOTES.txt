Mantis-DNS control plane deployed.

UI:      {{ include "mantis-dns.fullname" . }}-ui.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.ui.port }}
Control: {{ include "mantis-dns.fullname" . }}-control.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.control.port }}
{{- if .Values.ingress.enabled }}
Ingress: {{ if .Values.ingress.tls.enabled }}https{{ else }}http{{ end }}://{{ .Values.ingress.host }}
{{- end }}

Remember: mantis-dhcp and mantis-filter (edge DNS) are deployed separately —
see README "Production deploy". Point mantis-filter at the control Service
above via CONTROL_URL; mantis-dhcp talks to Postgres directly.

Log in with ADMIN_EMAIL / the ADMIN_PASSWORD from secret
"{{ .Values.secrets.existingSecret }}", then rotate it.
