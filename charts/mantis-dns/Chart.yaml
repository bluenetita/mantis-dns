apiVersion: v2
name: mantis-dns
description: Mantis-DNS control plane (control API + management UI). Filter nodes and Kea DHCP are deployed separately — see README "Production deploy".
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: postgresql
    version: "~16.2.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
