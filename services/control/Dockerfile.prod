FROM python:3.12-slim
WORKDIR /app

RUN groupadd -r aegis && useradd -r -g aegis aegis

COPY services/control .
RUN pip install --no-cache-dir . \
    && rm -rf /root/.cache

USER aegis
EXPOSE 8000
CMD ["uvicorn", "aegis_control.main:app", "--host", "0.0.0.0", "--port", "8000"]
