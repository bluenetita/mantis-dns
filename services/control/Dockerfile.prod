FROM python:3.12-slim
WORKDIR /app

RUN groupadd -r mantis && useradd -r -g mantis mantis

COPY services/control .
RUN pip install --no-cache-dir . \
    && rm -rf /root/.cache

USER mantis
EXPOSE 8000
CMD ["uvicorn", "mantis_control.main:app", "--host", "0.0.0.0", "--port", "8000"]
